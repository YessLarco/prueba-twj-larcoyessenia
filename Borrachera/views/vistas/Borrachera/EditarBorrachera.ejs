<h1>Crear Borracheras</h1>

<form action="/Borrachera/editarBorrachera" method="post">

    <div class="form-group">
        <label>Usuarios: </label>
        <select class="form-control" name="usuBorrachera">
           <option value="Seleccione un usuario">Seleccione un usuario</option>
            <% for(var i=0; i<usuarios.length; i++){ %>
            <option value="<%= usuarios[i].idUsuario%>" <%= (seleccionado == usuarios[i].idUsuario) ? 'selected' :''%>>
                <%= usuarios[i].nombreUsuario%></option>
            <%  } %>
        </select>
    </div>

    <div class="form-group">
       <input type="hidden" name="idBorrachera" value="<%= borrachera.idBorrachera%>">
        <label>Motivo: </label>
        <input type="text" class="form-control" placeholder="Ingrese el motivo de la borrachera" name="motivo" 
               value="<%= borrachera.motivo%>">
    </div>

    <div class="form-group">
        <label>Latitud donde empezó: </label>
        <input type="number" class="form-control" placeholder="Ingrese la latitud de su ubicación"  name="latitudDondeEmpezo" 
               value="<%= borrachera.latitudDondeEmpezo%>">
    </div>

    <div class="form-group">
        <label>Longitud donde empezó: </label>
        <input type="number" class="form-control" placeholder="Ingrese la longitud de su ubicación"  name="longitudDondeEmpezo"
               value="<%= borrachera.longitudDondeEmpezo%>">
    </div>

    <button type="submit" class="btn btn-success">Editar Borrachera</button>
</form>